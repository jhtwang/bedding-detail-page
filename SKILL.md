---
name: bedding-detail-page
description: 制作乐优家床品四件套电商详情页：资料预填、素材清单、LibTV 分模块出图、拼版、合规、切片。适用于用户给产品文件夹/品名后要求「填表 / 生成表格 / 做详情页」等场景。
---

# 床品四件套详情页制作

## 适用范围
仅乐优家床品**四件套**；枕头/被芯等其它类目需先另建「类目素材清单 + 类目排版」再走流程。

## 触发词
- 「填表 / 填一下表 / 生成表格 / 资料预填」→ 执行**前置步骤：资料预填**
- 「做一下详情页 / 制作详情页」→ 执行完整流程

## 前置步骤：资料预填
1. 产品文件夹名 = 品名
2. 到 `Y:\AI资料库\`（**只读，禁止修改**）按品名读取产品信息（26SS 培训 PPT、26AW 培训资料、产品推荐表-全表 等 pdf/pptx）
3. 调用 `references/四件套详情页素材清单_模板.xlsx` 自动填写 ①基本信息/材质工艺/规格 等
4. 保存到产品文件夹：`四件套详情页素材清单_<品名>.xlsx`
5. **交商家审核 + 填写图片信息**（③表指定图片等）
6. 商家确认后进入流程

## 流程
资料预填 → 素材准备 → 信息确认 → 文案定稿 → LibTV分模块出图 → 审图迭代 → 拼版合成 → 合规终审 → 切片交付 → 上架

### 9 模块（固定，勿增删）
| # | 模块 | 生成方式 |
|---|---|---|
| 1 | 首屏 | LibTV（顶部大标题=①第2项商品标题；底部卖点=①第24项一句话卖点；无副标） |
| 2 | 品牌理念 | 定稿模板（亮/深版），不走 LibTV |
| 3-5 | 卖点1/2/3 | LibTV（透明底产品图，固定提示词，产品零改动） |
| 6 | 细节实拍 | LibTV（2×2 宫格，细节图1-4 拼合成图作参考） |
| 7 | 场景大图 | **本地实拍直接做，不走 LibTV**（③表指定图或 CLIP 选图 → 等比缩放+居中裁剪调成 3:4 1440×1920 → Pillow 底部小字） |
| 8 | 规格材质 | LibTV 文生图（规格表 + 材质工艺；适配被芯=被套等大，固定项） |
| 9 | 洗涤护理 | LibTV 文生图（5 圆图标通用模板） |
| — | 底部备注 | Pillow（1440×720，免责，不做 3:4） |

### 统一风格前缀（每个 LibTV 提示词开头固定）
```
电商详情页长图，高端家居床品品牌「<品牌名>」（取素材清单①第1项，示例：乐优家），产品「<产品名>」，<风格描述>，<配色>，<风格元素>，画面干净高级、留白充足、柔和自然光，8K质感。画面文字一律使用免费可商用字体，禁止使用商业字体。
```

### 卖点固定提示词（卖点1/2/3 均一致，仅替换标题与副文案）
```
单卖点海报，3:4。参考图中的透明底产品必须100%原样保留：花型、颜色、图案、褶皱、细节与参考图完全一致，禁止改变产品外观。不改变产品的情况下，可为产品生成新的简约高级背景。画面中自然排布大字卖点「...」与副文案「...」，不限定左右位置，干净高级，不要LOGO。
```

### LibTV 出图
- 固定组合：`-s "model=Lib Image" -s "template=一键电商爆款产品详情页长图整套全案设计" -s modeType=image2image -s quality=high -s resolution=2K -s ratio=3:4`
- 规格/洗涤：`-s modeType=text2image`（不传参考图，提示词明确不含产品图）
- 每模块：1 参考图（upload→--left）+ 1 提示词 + 统一风格前缀；细节=4 参考图拼 2×2 合成图作单参考
- 下载必带 `--without-ai-watermark --vip`
- 批量：`group create <组>` → `group use <组>` → `upload`（自动进组）→ `node create ... --left <参考> --run`（或建好节点后 `group <组> --run` 整组生成）→ 完成后 `group unuse` → `download`
- 登录过期：`libtv login web`（浏览器授权）或 `login phone`
- 画布：`LibTV画布\`（已绑定「我的画布」）

### 拼版与切片
- 所有图片**等比缩放禁止拉伸变形**；场景统一调成 3:4（1440×1920）
- Pillow 拼版 1440 宽；AI 文案直接使用（不 Pillow 覆盖，AI 字体问题不处理）；底部免责 Pillow
- **切片按模块切**（一个模块一张；如平台限高≤960px 再在模块内拆分）

### 合规红线（详见 references/03_合规边界.md）
- 极限词/绝对化用语一律不写；支数/密度/克重与检测一致、无检测不写；成人A类（GB18401）≠婴幼儿A类（GB31701）、不写"母婴可用"；功效词（抗菌/防螨/助眠等）需报告；价格不虚构；AI 图不冒充实物；场景非卖品注明"仅为搭配展示"；免责注明（误差1-3cm/颜色以实物为准/不含枕芯被芯）；字体只用免费可商用（白名单/黑名单见 03）

## 参考与工具
- `references/00_README.md ~ 05_工具与素材规范.md`：完整规则（平台规范/固定排版/合规边界/制作流程/工具规范）
- `references/四件套详情页素材清单_模板.xlsx`：素材清单模板（新款式复制）
- `scripts/`：拼版切片、场景3:4、LibTV 批量命令等辅助脚本
- 本地工具：LibTV CLI、Pillow（`图片裁剪\.venv`）、rembg（抠图）、CLIP/ollama_see（选图/审图）